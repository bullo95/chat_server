# Initial configuration for certificate acquisition
server {
    listen 80;
    server_name backend.vigilys.fr;
    
    location /.well-known/acme-challenge/ {
        root /var/www/html;
        try_files $uri =404;
    }

    location / {
        return 200 'Ready for SSL certificate acquisition';
    }
}

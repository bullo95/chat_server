version: '3.8'

services:
  app:
    build: .
    ports:
      - "61860:61860"
    environment:
      - PORT=61860
      - DB_HOST=db
      - DB_USER=root
      - DB_PASSWORD=root_password
      - DB_NAME=dating_app
      - GIPHY_API_KEY=${GIPHY_API_KEY}
      - SERVER_IP=${SERVER_IP}
      - PUBLIC_VAPID_KEY=BKYeprBj9cKoYVNNiG_fJteHy5XVTrSor5ZEIVRxxyQtVIgK6hTozfN9qLi9IgoJTaCA4CbFlqXV
      - PRIVATE_VAPID_KEY=wCT9gl5kV3CLwiOpQUp7mVY7eAAVe24g6-KXVKVjknI
      - EMAIL=${EMAIL}
    volumes:
      - ./uploads:/usr/src/app/uploads
    depends_on:
      - db

  db:
    image: mysql:8.0
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=tcukeb6-tcukeb6
      - MYSQL_DATABASE=dating_app
    volumes:
      - mysql_data:/var/lib/mysql
      - ./database.sql:/docker-entrypoint-initdb.d/database.sql

volumes:
  mysql_data:

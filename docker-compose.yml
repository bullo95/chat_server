services:
  app:
    build: .
    ports:
      - "61860:61860"
    environment:
      - PORT=61860
      - DB_HOST=db
      - DB_USER=root
      - DB_PASSWORD=tcukeb6-tcukeb6
      - DB_NAME=dating_app
      - GIPHY_API_KEY=${GIPHY_API_KEY}
      - SERVER_IP=${SERVER_IP}
      - PUBLIC_VAPID_KEY=${PUBLIC_VAPID_KEY}
      - PRIVATE_VAPID_KEY=${PRIVATE_VAPID_KEY}
      - EMAIL=${EMAIL}
      - MYSQL_SSL_CA=/etc/mysql/certs/ca-cert.pem
      - MYSQL_SSL_CERT=/etc/mysql/certs/client-cert.pem
      - MYSQL_SSL_KEY=/etc/mysql/certs/client-key.pem
    volumes:
      - ./uploads:/usr/src/app/uploads
      - ./certs:/etc/mysql/certs:ro
    depends_on:
      db:
        condition: service_healthy

  db:
    image: mysql:8.0
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=tcukeb6-tcukeb6
      - MYSQL_DATABASE=dating_app
    command: >
      --ssl-ca=/etc/mysql/certs/ca-cert.pem
      --ssl-cert=/etc/mysql/certs/server-cert.pem
      --ssl-key=/etc/mysql/certs/server-key.pem
      --require-secure-transport=ON
    volumes:
      - mysql_data:/var/lib/mysql
      - ./database.sql:/docker-entrypoint-initdb.d/database.sql
      - ./certs:/etc/mysql/certs
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p$$MYSQL_ROOT_PASSWORD"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 30s

volumes:
  mysql_data:
